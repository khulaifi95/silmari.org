---
export interface Props {
  type: "BlogPosting" | "Person" | "WebSite";
  data: any;
}

const { type, data } = Astro.props;

let structuredData = {};

if (type === "BlogPosting") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    headline: data.title,
    description: data.description,
    author: {
      "@type": "Person",
      name: data.author || "Zhangda Xu",
      url: "https://silmari.org/about",
    },
    datePublished: data.pubDatetime.toISOString(),
    dateModified: data.modDatetime?.toISOString() || data.pubDatetime.toISOString(),
    publisher: {
      "@type": "Person",
      name: "Zhangda Xu",
      logo: {
        "@type": "ImageObject",
        url: "https://silmari.org/peter-avatar.jpg",
      },
    },
    mainEntityOfPage: {
      "@type": "WebPage",
      "@id": data.url,
    },
    image: data.ogImage || "https://silmari.org/peter-avatar.jpg",
    articleSection: data.tags?.[0] || "Technology",
    keywords: data.tags?.join(", ") || "",
    wordCount: data.wordCount,
    timeRequired: data.readingTime,
  };
} else if (type === "Person") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "Person",
    name: "Zhangda Xu",
    url: "https://silmari.org",
    image: "https://silmari.org/peter-avatar.jpg",
    sameAs: [
      "https://github.com/khulaifi95",
      "https://x.com/KhuLaiFiXu95",
      "https://www.linkedin.com/in/zhangdaxu/",
    ],
    jobTitle: "AI Practitioner",
    description:
      "My learning path as an AI practitioner. Every commit lands on GitHub for you to fork & remix.",
  };
} else if (type === "WebSite") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "WebSite",
    name: "Zhangda Xu",
    url: "https://silmari.org",
    description:
      "My learning path as an AI practitioner. Every commit lands on GitHub for you to fork & remix.",
    author: {
      "@type": "Person",
      name: "Zhangda Xu",
    },
    potentialAction: {
      "@type": "SearchAction",
      target: "https://silmari.org/search?q={search_term_string}",
      "query-input": "required name=search_term_string",
    },
  };
}
---

<script type="application/ld+json" set:html={JSON.stringify(structuredData)} />